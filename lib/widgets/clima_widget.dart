import 'package:flutter/material.dart';

class ClimaWidget extends StatelessWidget{
  const ClimaWidget({Key? key}) : super(key:key);

  @override
  Widget build(BuildContext context){
    return Container(

    );
  }
}